# 🎯 Automatización de Respuestas en Google Forms con Playwright

Este proyecto permite automatizar el llenado y envío de respuestas en un formulario de Google Forms, utilizando **Python** o **JavaScript** con **Playwright**.

---

## 📋 Requisitos Previos

Antes de ejecutar los scripts, asegúrate de tener instalado en tu sistema:

| Requisito       | Para Python                   | Para JavaScript (Node.js)      |
|:----------------|:-------------------------------|:-------------------------------|
| Python 3.x      | ✅                             | ❌                             |
| Pip             | ✅                             | ❌                             |
| Node.js y npm   | ❌                             | ✅                             |
| Playwright      | `pip install playwright`       | `npm install -D @playwright/test` |

---

## ⚙️ Instalación

### Para scripts en **Python** (Windows o Mac)

1. Instala Playwright:

   ```bash
   pip install playwright
   python3 -m playwright install

2. Clona o descarga el proyecto.

### Para scripts en **JavaScript (Node.js)** (Windows o Mac)
1. Instala Node.js:
2. Dentro del proyecto, instala Playwright:
   ```bash
   npm install -D @playwright/test
   npx playwright install

## ⚙️ Configuración

### Cantidad de respuestas
1. En Python (script.py) Ajusta el rango en:
   ```bash
   for i in range(50):
2. En JavaScript Ajusta el rango en: 
   ```bash
   for (let i = 0; i < 50; i++) { ... }

## 🚀 Ejecución

### Ejecutar el script en Python
   ```bash
   python script.py

### Ejecutar el script en JavaScript
   ```bash
   node script.js

## Ejecutar Testfile:
   ```bash
   npm run test





